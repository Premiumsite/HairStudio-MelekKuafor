const rain=document.getElementById("rain");const rainAmount=window.innerWidth<768?70:135;for(let i=0;i<rainAmount;i++){const drop=document.createElement("span");drop.className="drop";drop.style.left=Math.random()*110+"%";drop.style.animationDuration=.75+Math.random()*.9+"s";drop.style.animationDelay=Math.random()*2.8+"s";drop.style.opacity=(.18+Math.random()*.5).toString();drop.style.height=48+Math.random()*72+"px";rain.appendChild(drop)}
const header=document.getElementById("siteHeader");let lastScroll=0;window.addEventListener("scroll",()=>{const current=window.scrollY;if(current>lastScroll&&current>120){header.classList.add("hide")}else{header.classList.remove("hide")}lastScroll=current})
const reveals=document.querySelectorAll(".reveal");const observer=new IntersectionObserver((entries)=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("show")}})},{threshold:.16});reveals.forEach(el=>observer.observe(el))
const counters=document.querySelectorAll("[data-count]");let counterStarted=false;function runCounters(){if(counterStarted)return;const stats=document.querySelector(".stats");if(!stats)return;const rect=stats.getBoundingClientRect();if(rect.top<window.innerHeight-80){counterStarted=true;counters.forEach(counter=>{const target=Number(counter.dataset.count);let current=0;const steps=70;const increment=target/steps;const timer=setInterval(()=>{current+=increment;if(current>=target){counter.textContent=target;clearInterval(timer)}else{counter.textContent=Math.ceil(current)}},18)})}}window.addEventListener("scroll",runCounters);runCounters()
const glow=document.querySelector(".cursor-glow");window.addEventListener("mousemove",(e)=>{if(!glow)return;glow.style.left=e.clientX+"px";glow.style.top=e.clientY+"px"})
